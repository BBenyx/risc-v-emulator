# risc-v-emulator
A RISC-V emulator that supports some basic instructions.

In the riscv_sim folder you can find sample programs that I implemented the instructions for.
If you write a assembly with the same instructions it should work.
The memory the emulator works with has the size of 0.5 KB.
